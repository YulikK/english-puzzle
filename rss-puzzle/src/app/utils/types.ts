export type OptionsType = {
  onSound: boolean;
  onTranslate: boolean;
  onPicture: boolean;
} 

export enum HintName {
  onSound = 'onSound',
  onTranslate = 'onTranslate',
  onPicture = 'onPicture'
}

export enum FieldName {
  firstName = 'firstName',
  lastName = 'lastName'
}